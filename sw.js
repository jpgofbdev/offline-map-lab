const BASE = self.location.pathname.replace(/sw\.js$/, '');

const SHELL_ASSETS = [
  BASE,
  BASE + "index.html",
  BASE + "style.css",
  BASE + "offline.js",
  BASE + "regions.json",
  BASE + "style.json",
  BASE + "maplibre-gl.js",
  BASE + "maplibre-gl.css",
  BASE + "pmtiles.js"
];
